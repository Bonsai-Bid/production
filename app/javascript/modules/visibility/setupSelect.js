// import { handleSelectChange } from "./handleSelectChange";

// export function setupSelect(selectElement, otherElement) {
//   console.log("Setting up select with element:", selectElement);

//   if (!selectElement) {
//     console.error("selectElement is undefined or null.");
//     return;
//   }

//   handleSelectChange(selectElement, otherElement);

//   selectElement.addEventListener('change', () => {
//     console.log("Select changed to:", selectElement.value);

//     handleSelectChange(selectElement, otherElement);
//   });
// }
